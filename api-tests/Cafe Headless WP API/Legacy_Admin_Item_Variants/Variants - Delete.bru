meta {
  name: Variants - Delete
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/wp-json/cafe/v1/admin/items/{{itemId}}/variants/{{variantId}}
  body: none
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

settings {
  encodeUrl: true
  timeout: 0
}
