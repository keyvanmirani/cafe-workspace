meta {
  name: Tiles
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/wp-json/cafe/v1/tiles?branch={{branchSlug}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
test('Status is 200', function() { expect(res.status).to.equal(200); });
}
